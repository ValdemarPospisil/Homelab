global:
  scrape_interval: 15s # Jak často sbírat data

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100'] # Odkazuje na název kontejneru v Dockeru
